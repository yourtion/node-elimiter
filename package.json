{
  "name": "elimiter",
  "version": "0.2.0",
  "description": "easy node rate limiter",
  "keywords": [
    "rate-limiter",
    "ratelimit"
  ],
  "author": "YourtionGuo <yourtion@gmail.com>",
  "license": "MIT",
  "main": "dist/index.js",
  "typing": "dist/index.d.ts",
  "files": [
    "dist/index.d.ts",
    "dist/index.js"
  ],
  "scripts": {
    "test": "jest",
    "test:cov": "jest --coverage",
    "tag": "git tag v`node -p 'require(\"./package\").version'`",
    "format": "prettier --write \"src/**/*.ts\"",
    "clean": "rm -rf dist",
    "compile": "tsc",
    "prepublishOnly": "npm run format && npm run clean && npm run test:cov && cat ./coverage/lcov.info | coveralls",
    "postpublish": "npm run tag && git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourtion/node-elimiter.git"
  },
  "bugs": {
    "url": "https://github.com/yourtion/node-elimiter/issues"
  },
  "homepage": "https://github.com/yourtion/node-elimiter#readme",
  "devDependencies": {
    "@types/ioredis": "^3.2.13",
    "@types/jest": "^23.3.0",
    "@types/node": "^10.12.11",
    "coveralls": "^3.0.2",
    "ioredis": "^3.2.2",
    "jest": "^23.4.1",
    "prettier": "^1.13.7",
    "ts-jest": "^23.0.1",
    "typescript": "^2.9.2"
  },
  "peerDependencies": {
    "@types/node": "*"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testRegex": "./src/test",
    "collectCoverageFrom": [
      "src/index.ts"
    ],
    "coverageThreshold": {
      "global": {
        "branches": 80,
        "functions": 95,
        "lines": 80,
        "statements": 80
      }
    },
    "moduleFileExtensions": [
      "ts",
      "js",
      "json"
    ]
  }
}
